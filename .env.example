# Пример переменных. На сервере копируется в .env и заполняется реальными
# значениями. .env в git не попадает.
PORT=3000
DATA_DIR=/data
SITE_URL=https://salegifts.ru
# Пароль входа в админку. По умолчанию в коде стоит admin123 —
# оставлять его в проде нельзя.
ADMIN_PASSWORD=
