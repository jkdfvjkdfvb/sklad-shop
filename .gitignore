node_modules/
*.tmp
.env
.env.local
.env.production
# Исходные файлы импорта (большие, в git не нужны)
Контент/
scripts/__pycache__/
# Локальная копия персистентной папки загрузок (в проде — Railway Volume)
server/data/uploads/
# SSL-сертификат и приватный ключ, скачанные с reg.ru для ручной установки
reg_fullchain.cer
reg_privkey.key
# Инструкция по деплою на Reg.ru содержит живые пароли SSH и SFTP открытым
# текстом. После переезда на собственный VPS (27.08.2026) доступы подлежат
# отзыву, а файл — удалению; до тех пор он не должен попасть в историю.
DEPLOY-REG.md
