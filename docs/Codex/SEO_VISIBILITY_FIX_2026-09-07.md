# Исправление поисковой видимости salegifts.ru — 7 сентября 2026 года

## Результат диагностики

Публичный сайт доступен по HTTPS и отвечает `200 OK`. После деплоя и синхронизации каталога `robots.txt` разрешает обход публичных страниц, а `sitemap.xml` содержит 87 уникальных URL: главную, каталог, 20 категорий и 65 видимых товаров. Полный каталог в постоянном Docker-томе содержит 69 товаров; четыре позиции скрыты в админке.

### Google Search Console

- Свойство: `sc-domain:salegifts.ru`, уровень доступа `siteOwner`.
- За 28 дней, с 8 августа по 4 сентября: 0 показов, 0 кликов.
- За 90 дней, с 7 июня по 4 сентября: 0 показов, 0 кликов.
- Sitemap отправлен 30 августа, скачан Google 7 сентября, ошибок и предупреждений нет.
- Отчёт sitemap показывает 0 проиндексированных URL из 90, но URL Inspection возвращает `PASS` и `INDEXING_ALLOWED` для главной, каталога и большинства проверенных категорий. Это рассинхрон отчётов Search Console, а не подтверждение полного отсутствия страниц в индексе.
- Часть проверенных товарных URL и категория `/category/nabory-dlya-vina` ещё не имеют данных обхода.

### Яндекс Вебмастер

- Хост `https:salegifts.ru:443` добавлен и подтверждён.
- ИКС: 0.
- Страниц в поиске: 1 — главная `https://salegifts.ru/`.
- За 90 дней нет запросов, показов и кликов.
- Фатальных и критических проблем нет. DNS, HTTPS, robots.txt, sitemap и ответ главной без ошибок.
- Единственная активная рекомендация: `BIG_FAVICON_ABSENT`.
- Sitemap зарегистрирован без ошибок, но содержит `urls_count=0`; последнее чтение Яндексом было 29 августа, до последнего изменения шаблона и карты сайта.

## Что исправлено и опубликовано

- Все 20 категорий получили ориентированные на спрос H1 и уникальные data-driven meta description.
- На категориях добавлены фактические блоки выбора: материалы, цвета, ассортиментные кластеры и ссылки на конкретные товары.
- На товарных страницах добавлен фактический обзор с характеристиками, ценой, остатком, артикулом и контекстными ссылками.
- Каталог категорий, соседние категории и похожие товары перенесены из служебных `nav`/`aside` в основные контентные секции.
- У ссылок-изображений добавлены текстовые доступные названия; пустые анкоры устранены.
- Добавлены `CollectionPage` для каталога и категорий, `Organization` для главной и `seller` в товарных `Offer`.
- Устаревший `FAQPage` JSON-LD удалён; видимый FAQ сохранён.
- Для PNG 180×180 добавлено явное объявление favicon на всех шаблонах.
- `lastmod` шаблонных страниц обновлён до 7 сентября 2026 года, потому что изменился видимый контент.
- SEO-регрессия расширена с 10 до всех 20 категорий.

## Проверка

```powershell
$env:SEO_TEST_BASE='http://127.0.0.1:3000'
node scripts/verify-seo.js
```

Результат после синхронизации: `PASS` для 69 товаров полного каталога, 65 публичных карточек и 20 категорий; 65 уникальных Title, 65 уникальных Description, паритет цены и остатка между HTML, JSON-LD, `llms.txt` и API. `node --check` и `git diff --check` также проходят.

В браузере проверены главная, `/category/breloki` и товарная карточка. На мобильном viewport 390 px горизонтального переполнения нет.

## Действия после деплоя — 8 сентября 2026 года

- Новые шаблоны подтверждены на продакшене по главной, категории и товарной карточке: HTTP 200, новые контентные блоки и schema присутствуют.
- Приоритетный переобход зарегистрированного sitemap запущен в Яндекс Вебмастере: использован 1 запрос из месячной квоты 10.
- На обычный переобход отправлены 22 основные страницы: главная, каталог и все 20 категорий. Все задачи приняты и находятся в состояниях `DONE` или `IN_PROGRESS`.
- Цены, остатки, видимость и связанные складские поля всех 69 товаров скопированы из постоянного Docker-тома продакшена в локальный `server/data/products.json`; финальная сверка дала 0 несовпадений по восьми полям.

## Что сознательно не сделано

- Новые массовые pSEO-страницы не создавались. Семь категорий содержат только по одному товару; дополнительная генерация сейчас повысит риск thin/doorway pages.
- Четыре скрытых на продакшене товара не удалялись и не обнулялись: локально сохранены их реальные цена и остаток, а `visible=false` исключает их из публичного API и sitemap.

## НЕТ ДАННЫХ

Не подтверждены юридическое наименование продавца, регион работы, правила оплаты и доставки, возвраты, минимальный оптовый заказ, условия нанесения логотипа, отзывы и внешние доказательства. Эти сведения не добавлялись в текст или schema.

## Следующий безопасный шаг

1. Через 3–14 дней проверить новое чтение sitemap, число страниц в поиске и завершение очереди переобхода.
2. Отслеживать показы отдельно в Google Search Console и Яндекс Вебмастере.
3. Перед следующим деплоем снова считать постоянный Docker-том источником истины для цены, остатка и видимости.
